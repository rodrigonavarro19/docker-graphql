name: Docker Image CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3
      with:
        fetch-depth: 0
    - name: Git Semantic version
      uses: PaulHatch/semantic-version@v4.0.3
      with:
        major_pattern: "major:"
        minor_pattern: "feat:"
        format: "${major}.${minor}.${patch}-prerelease${increment}"
      id: version
      
    - name: Docker login
      env:
        DOCKER_USER: ${{ secrets.DOCKER_USER }}
        DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
        NEW_VERSION: ${{ steps.version.outputs.version }}
      run: |
        docker login -u $DOCKER_USER -p $DOCKER_PASSWORD
        echo "New Version: $NEW_VERSION !!!!!!!"

    # - name: Build Docker Image
    #   run: |
    #     docker build -t rodrigonavarro19/docker-graphql:0.0.2 .
    #     docker build -t rodrigonavarro19/docker-graphql:latest .
        
    # - name: Push Docker Image
    #   run: |
    #     docker push rodrigonavarro19/docker-graphql:0.0.2
    #     docker push rodrigonavarro19/docker-graphql:latest
        

